rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default rule: Allow public read access to all files, but only allow
    // authenticated users to write (create, update, delete) files.
    match /{allPaths=**} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}
